# Local plugin config (copy of config.inc.php.dist) — never commit a real config
config.inc.php

# Composer
/vendor/
composer.lock

# Editor / OS cruft
*.swp
*~
.DS_Store
.idea/
.vscode/

# Playwright MCP scratch output (headless UI checks)
.playwright-mcp/
